#include<iostream>
#include<cstdlib>

using namespace std;

int main()
{
    int j;
    bool v;
    int M[5];
    int N[5];
    for( int i=0;i<=4;i++)
        cin>>N[i];

    for( int i=0;i<=4;i++)
        cin>>M[i];



   /* do{

        for(j=0;j<=4;j++)

            if(N[j]!=M[j])
                v=false;
            else
                v=true;
       }
       while(v);
       if(v=true)
       cout<<"vectorii sunt identici";
       else
       cout<<"vectorii nu sunt identici";*/

}
